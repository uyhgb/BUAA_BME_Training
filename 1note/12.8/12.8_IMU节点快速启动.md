两个必要的source
source /opt/ros/humble/setup.bash && source install/setup.bash


# win系统开发
映射usb设备到wsl2
启动容器前
usbipd attach --wsl --busid 2-3


测试USB口
ls -l /dev/ttyUSB0

配置波特率并直接读取数据
stty -F /dev/ttyUSB0 115200 raw -echo && timeout 3 cat /dev/ttyUSB0 2>&1 | head -30

直接用python读取数据
python3 -c "import serial; ser = serial.Serial('/dev/ttyUSB0', 115200, timeout=1); print('串口打开成功'); import time; time.sleep(0.5); data = ser.readline(); print(f'读取数据: {data}'); ser.close()" 2>&1

测试ROS2功能包
cd /workspace/robot_ws && source /opt/ros/humble/setup.bash && source install/setup.bash && timeout 10 ros2 launch exo_sensors imu_csv_dev_wsl.launch.py 2>&1 | head -50

# 如果 ros2 topic list 报错，重启 daemon
ros2 daemon stop && ros2 daemon start

# 采集数据
不一定需要 `timeout`！有两种方式：

## 方式1: 使用 timeout（固定时长采集）
```bash
cd /workspace/robot_ws && source /opt/ros/humble/setup.bash && source install/setup.bash && timeout 10 ros2 launch exo_sensors imu_complete_system.launch.py enable_recorder:=true enable_visualizer:=false
```
- **优点**: 自动停止，适合批量采集固定时长的数据
- **缺点**: 必须提前确定时长

## 方式2: 手动停止（推荐日常使用）
```bash
cd /workspace/robot_ws && source /opt/ros/humble/setup.bash && source install/setup.bash && ros2 launch exo_sensors imu_complete_system.launch.py enable_recorder:=true enable_visualizer:=false
```
- **操作**: 启动后做你要采集的动作，完成后按 **Ctrl+C** 停止
- **优点**: 灵活控制采集时长，适合采集不同长度的步态数据
- **缺点**: 需要手动停止

---

## 📋 实际采集数据流程建议

### 采集不同步态的训练数据
```bash
# 1. 站立数据（30秒）
timeout 30 ros2 launch exo_sensors imu_complete_system.launch.py \
    enable_recorder:=true enable_visualizer:=false
# 完成后手动重命名文件为: imu_standing_xxx.csv

# 2. 行走数据（60秒）
timeout 60 ros2 launch exo_sensors imu_complete_system.launch.py \
    enable_recorder:=true enable_visualizer:=false
# 完成后重命名为: imu_walking_xxx.csv

# 3. 跑步数据（30秒）
timeout 30 ros2 launch exo_sensors imu_complete_system.launch.py \
    enable_recorder:=true enable_visualizer:=false
# 完成后重命名为: imu_running_xxx.csv
```

### 或者灵活手动控制
```bash
# 启动采集
ros2 launch exo_sensors imu_complete_system.launch.py \
    enable_recorder:=true enable_visualizer:=false

# 做各种动作...
# 完成后按 Ctrl+C 停止
```

---

## 💡 建议

**日常采集**: 不用 `timeout`，手动 Ctrl+C 停止更灵活  
**批量采集**: 用 `timeout` 可以保证每次时长一致，便于数据对齐

数据会自动保存到 `robot_ws/data/imu_gait_data_时间戳.csv`，记得及时重命名标注步态类型！