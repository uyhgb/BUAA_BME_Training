文件关系
1️⃣ imu_csv_reader.launch.py - 通用版本
作用: 最基础的启动文件，使用通用配置 imu_csv_reader.yaml
用途: 可以通过命令行参数快速指定串口
适用场景: 临时测试、不同平台快速切换
2️⃣ imu_csv_dev_wsl.launch.py - WSL2开发环境专用
作用: 预配置好WSL2环境参数，使用 imu_csv_dev_wsl.yaml
用途: 你现在用的就是这个！固定 ttyUSB0，适合日常开发
适用场景: Windows + WSL2 + Docker + usbipd-win
3️⃣ imu_csv_raspberry_pi.launch.py - 树莓派生产环境专用
作用: 预配置树莓派参数，使用 imu_csv_raspberry_pi.yaml，启用自动检测
用途: 部署到树莓派后直接用
适用场景: Raspberry Pi 实际硬件



`imu_complete_system.launch.py` 是**一键启动完整系统**，同时启动三个节点：

## 🎯 完整系统架构

```
┌─────────────────────────────────────────────┐
│   imu_complete_system.launch.py            │
└─────────────────────────────────────────────┘
            │
            ├──► imu_csv_reader (发布端)
            │       ↓
            │    读取串口 → 发布 /imu/data
            │       ↓
            ├──► imu_data_recorder (订阅端1)
            │       ↓
            │    订阅 /imu/data → 保存到 CSV 文件
            │
            └──► imu_visualizer (订阅端2)
                    ↓
                 订阅 /imu/data → 实时绘制曲线
```

---

## 📋 三个节点的作用

| 节点 | 角色 | 功能 |
|------|------|------|
| **imu_csv_reader** | 发布端 | 读取串口→发布话题 |
| **imu_data_recorder** | 订阅端1 | 记录数据到文件 |
| **imu_visualizer** | 订阅端2 | 实时可视化 |

---

## 🚀 使用场景

### 场景1: 只需要实时查看数据（你现在）
```bash
ros2 launch exo_sensors imu_csv_dev_wsl.launch.py
```
只启动 `imu_csv_reader`，用 `ros2 topic echo` 手动查看

### 场景2: 需要采集训练数据
```bash
ros2 launch exo_sensors imu_complete_system.launch.py \
    enable_recorder:=true \
    enable_visualizer:=false
```
启动读取器 + 记录器，数据自动保存到 `data/` 目录

### 场景3: 需要实时监控 + 保存数据
```bash
ros2 launch exo_sensors imu_complete_system.launch.py \
    enable_recorder:=true \
    enable_visualizer:=true
```
三个节点全开，边看边记录

### 场景4: 只看可视化不保存
```bash
ros2 launch exo_sensors imu_complete_system.launch.py \
    enable_recorder:=false \
    enable_visualizer:=true
```

---

## 💡 简单理解

- **`imu_csv_dev_wsl.launch.py`** = 只开读取器（最轻量）
- **`imu_complete_system.launch.py`** = 一键全开（读取+记录+可视化）

你现在用 `imu_csv_dev_wsl.launch.py` 已经够了，等需要采集步态数据时再用 `imu_complete_system.launch.py`。