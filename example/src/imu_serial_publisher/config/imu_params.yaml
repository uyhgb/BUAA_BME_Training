# IMU串口发布器配置文件

# 串口参数
serial_port: "/dev/ttyUSB0"  # Windows下可能是 COM3, COM4 等
baud_rate: 115200

# 坐标系
frame_id: "imu_link"

# 发布频率 (Hz)
publish_rate: 20.0

# 如果需要,可以添加校准参数
# accel_offset: [0.0, 0.0, 0.0]
# gyro_offset: [0.0, 0.0, 0.0]
# mag_offset: [0.0, 0.0, 0.0]
